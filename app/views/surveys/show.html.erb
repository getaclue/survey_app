
          <div class="text-center panel">
          <%= form_for(current_survey) do |survey_form| %>
          <div class="row">
            <div class="large-12 columns">
              <div class="row collapse">
                <div class="small-10 columns">
                  <%= survey_form.text_field :title %>
                </div>
                <div class="small-2 columns">
                  <%= survey_form.submit "Edit Survey Title", class: "button postfix" %>
                </div>
              </div>
            </div>
            </div>
          <% end %>
          <%= link_to "Make new survey", root_url, class: "button" %>
          <%= link_to "Activate survey", '#', class: "button disabled" %>
          </div>

          <%= render 'survey_items/new_question', object: @makenewquestion %>

          <% @questions.each do |question| %>
          <div class="panel">
            <ul>
              <li>
                <%= question.content %> [<%= link_to "Edit Question", survey_question_path(current_survey, question.id) %>]
                <% if question.answers.any? %>
                  <ul>
                  <% @answers = question.answers %>
                  <%= render 'survey_items/survey_item', 
                                                collection: @answers %>
                  </ul>
                <% end %>
              </li>
            </ul>
          </div>
          <% end %>